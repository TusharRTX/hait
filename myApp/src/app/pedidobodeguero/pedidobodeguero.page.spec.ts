import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PedidobodegueroPage } from './pedidobodeguero.page';

describe('PedidobodegueroPage', () => {
  let component: PedidobodegueroPage;
  let fixture: ComponentFixture<PedidobodegueroPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PedidobodegueroPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
