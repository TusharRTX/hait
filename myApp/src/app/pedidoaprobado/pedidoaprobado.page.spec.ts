import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PedidoaprobadoPage } from './pedidoaprobado.page';

describe('PedidoaprobadoPage', () => {
  let component: PedidoaprobadoPage;
  let fixture: ComponentFixture<PedidoaprobadoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PedidoaprobadoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
