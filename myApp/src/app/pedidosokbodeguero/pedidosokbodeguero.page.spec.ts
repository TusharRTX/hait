import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PedidosokbodegueroPage } from './pedidosokbodeguero.page';

describe('PedidosokbodegueroPage', () => {
  let component: PedidosokbodegueroPage;
  let fixture: ComponentFixture<PedidosokbodegueroPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PedidosokbodegueroPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
